export default function Footer() {
  return (
    <footer className="border-t border-white/10 mt-20">
      <div className="mx-auto max-w-7xl px-4 md:px-6 py-10 flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-mist">
        <p>SignBridge — Breaking Communication Barriers with AI. An exhibition prototype.</p>
        <p>Runs entirely on-device: no login, no paid APIs, no cloud storage of camera footage.</p>
      </div>
    </footer>
  );
}
